\n@@ -14,7 +14,7 @@\n const EMPTY: UserForm = { name: '', email: '', phone: '', role: 'Staff', status: 'Active' };\n \n export default function UsersList() {\n-  const { users, branches, centres, addUser, updateUser } = useMockDb();\n+  const { users, branches, centers, addUser, updateUser } = useMockDb();\n   const [searchTerm, setSearchTerm] = useState('');\n   const [viewItem, setViewItem] = useState<User | null>(null);\n   const [editItem, setEditItem] = useState<User | null>(null);\n@@ -29,7 +29,7 @@\n \n   const getLocation = (u: User) => {\n     if (u.branchId) return branches.find(b => b.id === u.branchId)?.name || '';\n-    if (u.centreId) return centres.find(c => c.id === u.centreId)?.name || '';\n+    if (u.centreId) return centers.find(c => c.id === u.centreId)?.name || '';\n     return 'Global Access';\n   };\n \n